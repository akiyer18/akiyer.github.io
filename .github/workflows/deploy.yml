# DISABLED - Use manual deployment with `npm run deploy` instead
# GitHub Actions was having permission issues with the repository

name: Deploy Growth Mindset Tools to GitHub Pages (DISABLED)

# Disabled - no triggers
on:
  workflow_dispatch: # Only manual trigger, never automatic

# # Add permissions for GitHub Pages deployment
# permissions:
#   contents: read
#   pages: write
#   id-token: write

# # Allow only one concurrent deployment
# concurrency:
#   group: "pages"
#   cancel-in-progress: false

# jobs:
#   build:
#     runs-on: ubuntu-latest
    
#     steps:
#     - name: Checkout repository
#       uses: actions/checkout@v4
      
#     - name: Setup Node.js
#       uses: actions/setup-node@v4
#       with:
#         node-version: '18'
#         cache: 'npm'
        
#     - name: Install dependencies
#       run: npm ci
      
#     - name: Build React application
#       run: npm run build
        
#     - name: Upload build artifacts
#       uses: actions/upload-pages-artifact@v3
#       with:
#         path: './dist'

#   deploy:
#     environment:
#       name: github-pages
#       url: ${{ steps.deployment.outputs.page_url }}
#     runs-on: ubuntu-latest
#     needs: build
    
#     steps:
#     - name: Deploy to GitHub Pages
#       id: deployment
#       uses: actions/deploy-pages@v4 

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
    - name: Deployment disabled
      run: echo "This workflow is disabled. Use 'npm run deploy' for manual deployment." 